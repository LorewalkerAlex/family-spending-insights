# App 截图账单与邮箱 CSV 映射工具实施计划

## 目标

实现一个可重复运行的本地命令行工具。工具读取记账 App 的账单长截图和招商银行邮箱账单 CSV，通过 OCR、结构解析和一对一匹配，产出逐笔匹配结果、待人工确认记录，以及可在后续月份复用和累积的商户映射表。

首个验收样本为项目中的 6 月账单截图与 `cmb_bill_output/2026-06-10-cmbbilling-raw.csv`。忽略截图中的消费者姓名。

## 约束

- 沿用项目现有 `uv`、`.venv`、`pyproject.toml` 和 `uv.lock`。
- 保持 Python 3.14，不另建 OCR 专用环境。
- OCR 默认在本机 CPU 上运行，不依赖在线 API。
- 原始截图和原始 CSV 只读；所有结果写入独立输出目录。
- 人工确认过的映射优先级最高，后续运行不得自动覆盖。
- 相同输入重复运行必须得到稳定结果，不重复累积同一条规则。

## 技术选择

优先采用 RapidOCR + ONNX Runtime，使用适合中英文与数字的 PP-OCR 系列模型。原因是依赖较轻、可本地 CPU 推理，并且 ONNX Runtime 提供 Windows Python 3.14 轮子。

如果实际安装或运行验证失败，则在不改变现有 Python 版本的前提下选择兼容的 ONNX OCR 实现。不会通过另建环境掩盖兼容问题。

## 首版实现策略

先实现一个可独立运行的 `build_mapping.py`，快速完成端到端验证。只有在流程稳定、模块边界已经由实际代码验证后，才按需要拆分为包，避免过早抽象。

命令行形式：

```powershell
uv run python build_mapping.py `
  --image "微信图片_20260711202903_39_5.jpg" `
  --csv "cmb_bill_output/2026-06-10-cmbbilling-raw.csv"
```

可选参数包括输出目录、已有 mapping 路径、OCR 调试结果保存、匹配阈值，以及只执行 OCR/只执行匹配的模式。

## 数据处理流程

### 1. 截图预处理与 OCR

- 读取超长截图并按高度切成带重叠的图块，避免 OCR 缩小整图导致小字丢失。
- 对每个图块执行 OCR，保存文字、置信度和坐标。
- 将局部坐标转换成整图坐标。
- 对重叠区域识别结果去重。
- 保留可审计的 OCR 中间 JSON，便于定位误识别。

### 2. App 交易结构解析

- 根据纵坐标聚合属于同一交易卡片/行的文字。
- 识别金额列，并以金额所在行作为交易锚点。
- 从同一纵向区间提取商户、账户、分类等文本。
- 排除页面标题、汇总、消费者姓名和底部导航。
- 输出标准化的 App 交易表，并保留 OCR 原文与位置。

### 3. 邮箱 CSV 标准化

- 读取交易日、入账日、原始描述、金额、卡尾号。
- 从描述中拆分支付渠道和银行侧商户主体。
- 统一金额符号、空格、标点、大小写及常见公司后缀。
- 根据账单文件名推断跨年后的完整交易日期。

### 4. 候选生成与一对一匹配

- 金额一致是主要候选条件，并容忍明确可控的小数识别误差。
- 综合商户文本相似度、渠道、日期、交易顺序、已有 mapping 和 OCR 置信度评分。
- 使用全局一对一分配，防止同一 App 交易匹配多条 CSV。
- 对相同金额、多候选且证据不足的情况不强行匹配。

### 5. 结果分层

- `confirmed`：证据充分，可进入长期 mapping。
- `probable`：建议匹配，需要抽查或人工确认。
- `ambiguous`：存在多个合理候选。
- `unmatched_csv`：仅存在于邮箱 CSV。
- `unmatched_app`：仅存在于 App 截图。
- `ocr_error`：截图字段不足以形成有效交易。

### 6. Mapping 累积

Mapping 至少包含：

- `source_pattern`
- `source_channel`
- `card_last4`
- `canonical_merchant`
- `app_account`
- `category`
- `sample_count`
- `confirmed_count`
- `first_seen`
- `last_seen`
- `rule_status`
- `manually_confirmed`

规则以稳定的规范化键去重。手动确认规则只允许显式修改，自动运行只能更新统计和时间范围。

## 输出

默认写入 `mapping_output/<账单周期>/`：

- `ocr_results.json`：带位置和置信度的 OCR 原始结果。
- `app_transactions.csv`：从截图解析出的结构化交易。
- `matched_transactions.csv`：CSV 与 App 的逐笔配对、分数和证据。
- `review_required.csv`：需要人工确认或未匹配的记录。
- `merchant_mapping.csv`：可复用映射表。
- `run_summary.json`：输入、数量、匹配率、冲突和运行参数摘要。

## 验证与验收

1. OCR 后的交易金额与截图抽样一致。
2. App 交易拆行无明显合并、错行和重复。
3. 邮箱 CSV 的 101 条记录全部进入匹配或未匹配结果，没有静默丢失。
4. 一条 App 交易不会匹配多条 CSV。
5. 高置信度匹配抽样核验商户、账户和分类。
6. 模糊匹配必须给出候选或明确原因。
7. Mapping 只吸收可靠结果，不因低置信度匹配受到污染。
8. 相同输入重复运行，输出稳定且 mapping 不产生重复规则。
9. 提供最小自动测试，覆盖金额解析、描述规范化、跨月日期、一对一匹配和 mapping 去重。

## 实施顺序

1. 验证并锁定 OCR 依赖。
2. 实现长图切片、OCR 坐标合并和中间结果缓存。
3. 基于真实截图实现 App 交易解析。
4. 实现 CSV 标准化和账单周期日期推断。
5. 实现候选评分与一对一匹配。
6. 实现输出文件和 mapping 累积逻辑。
7. 用 6 月样本端到端运行并统计误差。
8. 针对真实错误优化参数与规则。
9. 补充自动测试、README 使用说明和重复运行验证。

## 调整原则

实际 OCR 布局、字段名称或依赖兼容性可能与初步观察不同。执行时允许调整切片、解析、评分和文件组织，但必须保持以下目标不变：现有 uv 环境可运行、原始数据不被修改、结果可审计、匹配一对一、mapping 可复用且可人工纠错。
