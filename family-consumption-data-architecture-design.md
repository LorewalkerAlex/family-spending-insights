# 家庭消费数据系统框架设计说明

## 1. 文档目的

本文用于定义当前家庭消费数据系统的整体框架、核心边界和数据关系。

本设计聚焦于以下问题：

- 消费数据如何获取、清洗和长期保存；
- 银行侧账户名如何被转换为可理解、可分析的商家与标签；
- 各阶段产物之间如何依赖；
- 上游发生变化后，哪些下游结果需要重新生成；
- 分析、图表和 AI 报告在整体系统中的位置。

本文不讨论具体文件格式、目录结构、编程语言、数据库、部署方式、任务调度技术、统计公式或 Prompt 内容。

---

## 2. 当前目标

当前系统的核心目标是：

> 基于招商银行信用卡账单，形成一套长期可维护、可重建的本地消费数据体系，并在此基础上按需生成统计、图表和 AI 消费报告。

长期可以扩展为更综合的家庭财务管理系统，但当前设计只围绕消费数据展开，不提前引入多账户、多银行、资产负债或复杂财务运营能力。

---

## 3. 总体框架

系统由四个主要部分组成：

```text
数据获取与清洗
        ↓
      Mapping
        ↓
     数据分析
        ↓
       展示
```

从实际依赖关系看，更准确的表达是：

```text
原始账单文件
        ↓
数据获取与清洗
        ↓
清洗后的交易数据 ───────┐
                        ├──→ 按需分析与消费
Mapping 数据 ───────────┘
                              ↓
                   统计 / 图表 / AI 报告 / 查询
                              ↓
                            展示
```

Mapping 不是必须全部完成后才能分析的前置门槛。未被 Mapping 识别的交易仍可继续进入分析，展示时使用原始账户名。

---

## 4. 核心设计原则

### 4.1 原始数据不可变

原始邮件、原始账单文件和从中提取的原始交易事实不被下游覆盖或修改。

所有商家归一、标签、统计和报告都是对原始数据的解释或派生产物，不能反向改变原始事实。

系统外部手工修改原始文件不属于本设计范围。

### 4.2 每一步产物都可以长期保存

清洗数据、语义化数据、分析结果、图表和 AI 报告都可以作为本地文件长期保存，以便直接读取和回顾。

但除原始数据和独立维护的 Mapping 外，其他产物都属于可重新生成的派生结果。

### 4.3 上游变化时，下游覆盖重建

当某一步的输入或处理逻辑发生变化时，从该步骤开始重新生成受影响的下游产物，并覆盖旧结果。

例如：

```text
原始数据处理逻辑变化
→ 重建清洗数据
→ 重新应用 Mapping
→ 重建分析、图表和报告
```

```text
Mapping 变化
→ 清洗数据保持不变
→ 重建语义化数据及其下游结果
```

```text
分析需求或 Prompt 变化
→ 上游交易数据保持不变
→ 只重建对应的分析或报告
```

### 4.4 文件是当前的持久化方式

当前数据量很小，因此所有数据和产物都保持本地文件存储。

文件存储是当前明确选择，不需要引入数据库或复杂服务。未来若存储规模或访问方式发生变化，可以替换持久化方式，但不改变本文定义的逻辑边界。

### 4.5 分析内容不是固定架构

分析、聚类、图表和 AI 报告会随着需求变化。

系统不需要预先设计一套固定的分析模块。分析端只需要读取交易数据与 Mapping，将其加载到内存中，然后按当前需求进行计算和输出。

---

## 5. 数据获取与清洗

### 5.1 职责

数据获取与清洗负责把外部账单转化为可靠、统一、可长期读取的交易数据。

它关注：

- 数据是否完整获取；
- 日期、金额、退款、卡号标识等基础事实是否正确；
- 相同数据是否被重复导入；
- 不同账单中的交易是否能被统一表达；
- 原始字段是否得到保留。

### 5.2 边界

这一部分只回答：

> 发生了哪些交易？

它不负责：

- 判断银行账户名实际对应哪个商家；
- 给交易赋予消费类别或标签；
- 生成消费结论；
- 生成图表或报告。

### 5.3 产物

清洗后的交易数据长期保存，作为后续 Mapping 和分析的稳定输入。

该数据可以从原始账单重新生成，因此仍属于可重建产物。

---

## 6. Mapping

### 6.1 定位

Mapping 是交易的语义补全层。

它不改变交易事实，而是为交易增加用户可理解、可统计的语义。

Mapping 完成两个核心工作：

1. 将银行侧原始账户名归一为用户认知中的标准商家；
2. 为标准商家赋予主标签和辅助标签，支持后续分析。

### 6.2 分层关系

```text
原始账户名
    ↓
标准商家
    ↓
主标签
    ↓
多个辅助标签
```

示例：

```text
某网络科技有限公司
某电子商务有限公司
某支付渠道下的收款账户
        ↓
       盒马
        ↓
主标签：商超
辅助标签：食品、家庭消费、日常消费
```

### 6.3 账户名到商家的约束

账户名到标准商家的关系必须满足：

- 一个原始账户名只能映射到一个标准商家；
- 多个原始账户名可以映射到同一个标准商家；
- 不允许一个原始账户名同时映射到多个标准商家；
- 无法可靠确认时，不强行映射，继续保留原始账户名。

也就是：

```text
账户名 → 商家：一对一或多对一
```

### 6.4 标签关系

标准商家可以拥有：

- 一个主标签，用于稳定、互斥或主要口径的统计；
- 多个辅助标签，用于交叉观察、聚类、筛选和 AI 分析。

标签体系可以根据后续需求变化，不需要在当前框架阶段一次性确定完整内容。

### 6.5 Mapping 的维护方式

Mapping 的最终沉淀物是一份本地文件。

它可以通过多个渠道维护：

- App 图片批量识别工具；
- 人工逐条补充工具；
- 直接编辑 Mapping 文件；
- 未来的其他辅助工具。

所有工具最终调用同一个 Mapping 修改入口，避免不同入口各自维护一套写入逻辑。

### 6.6 与核心系统的边界

Mapping 生产工具是独立工具。

核心消费分析系统只负责读取 Mapping 文件，不负责：

- 图片 OCR；
- 图片与账单交易匹配；
- Mapping 候选审核；
- Mapping 文件的交互式维护流程。

Mapping 工具可以独立替换、扩展或废弃，而不影响消费数据主链路。

---

## 7. 数据分析与消费入口

### 7.1 定位

数据分析不是一套固定的长期流程，而是一组按需变化的消费者。

统一入口的逻辑是：

```text
读取清洗后的交易数据
+ 读取当前 Mapping
= 得到语义化交易集合
```

之后根据具体需求进行：

- 数值统计；
- 商家或标签维度汇总；
- 时间对比；
- 聚类分析；
- 图表生成；
- AI 报告生成；
- 流水查询；
- 其他临时或长期分析。

### 7.2 AI 报告的位置

AI 报告本质上是分析结果的一种消费方式。

确定性的事实、数值和基础统计由程序计算；AI 通过 Prompt 读取这些结构化结果，生成自然语言消费报告。

Prompt 的具体设计、报告风格和分析角度不属于整体架构问题，可以独立迭代。

### 7.3 消费判断不沉淀为长期规则

系统不保存“日常消费”“冲动消费”“合理消费”等长期个人判断。

原因是家庭收入、生活阶段和消费认知会持续变化。AI 可以针对当期数据生成判断，但这些判断只属于当期报告，不反向进入 Mapping 或交易数据体系。

---

## 8. 展示

展示是分析结果的出口，不是数据处理中心。

当前展示载体可以是小程序，主要用于：

- 查看月度消费报告；
- 查看历史报告；
- 查看交易流水；
- 查看统计和图表；
- 进入独立 Mapping 工具提供的人工补充入口。

展示层不重新定义交易事实，也不维护独立的统计口径。

未来即使更换为网页、桌面应用或其他界面，也不影响上游数据结构和处理框架。

---

## 9. 数据资产与派生产物

系统中的数据可以分为三类。

### 9.1 不可变事实

- 原始邮件；
- 原始账单文件；
- 原始交易事实。

### 9.2 独立维护的语义资产

- 账户名到标准商家的 Mapping；
- 标准商家的主标签；
- 标准商家的辅助标签。

### 9.3 可重建的派生产物

- 清洗后的交易数据；
- 应用 Mapping 后的语义化交易数据；
- 统计结果；
- 聚类或对比结果；
- 图表；
- AI 报告；
- 展示端所需的其他数据文件。

这些派生产物可以长期保存，但上游变化后允许重新生成并覆盖。

---

## 10. 整体依赖关系

```text
原始账单文件（不可变）
        ↓
数据获取与清洗
        ↓
清洗交易文件（长期保存，可重建）
        │
        ├────────────────┐
        │                │
        │          Mapping 文件
        │        （独立维护的语义资产）
        │                │
        └────────┬───────┘
                 ↓
          语义化交易集合
                 ↓
          按需分析与消费
       ┌─────────┼─────────┐
       ↓         ↓         ↓
     统计       图表      AI 报告
       └─────────┼─────────┘
                 ↓
                展示
```

更新方向始终从上游流向下游：

- 展示不能修改原始事实；
- 分析不能修改 Mapping；
- Mapping 不能覆盖清洗交易；
- 原始数据不依赖任何下游结果。

展示端可以调用独立 Mapping 工具提交商家修正，但这属于 Mapping 维护链路，不属于交易数据的反向修改。

---

## 11. 非目标

当前阶段明确不讨论或不建设：

- 多银行、微信或支付宝独立账单接入；
- 家庭资产负债管理；
- 数据库和复杂后端服务；
- 微服务或事件驱动架构；
- 固定的分析指标体系；
- 固定的图表体系；
- AI Prompt 的具体内容；
- 消费行为画像或长期冲动消费模型；
- 调度、部署、缓存和实时计算细节；
- 原始文件的编辑、审计或版本管理功能。

---

## 12. 设计结论

本系统采用：

> **本地文件驱动、分层持久化、依赖驱动重建的消费数据 Pipeline。**

其核心不是某个具体分析页面或 AI 报告，而是两类长期稳定的数据资产：

1. 可靠的交易数据；
2. 可持续维护的 Mapping 数据。

统计、图表、AI 报告和展示都是基于这两类资产产生的可变消费者。

该框架保持足够简单，适合当前很小的数据量和单一信用卡来源，同时保留了未来扩展分析方式和展示方式的空间，而不提前引入不必要的复杂度。
